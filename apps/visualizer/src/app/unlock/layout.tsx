import type { ReactNode } from 'react';

export const metadata = {
  title: 'Unlock',
  description: 'Redeem a torus Production License key.',
};

export default function UnlockLayout({ children }: { children: ReactNode }) {
  return children;
}
